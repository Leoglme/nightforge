# API base consumed by the dashboard.
# Left commented on purpose: when unset the app resolves automatically
#   - dev  -> http://localhost:8010
#   - prod -> https://api.nightforge.dibodev.fr (via NODE_ENV=production)
# DevLeadHunter utilise le port 8000 — ne pas le réutiliser ici.
# NUXT_PUBLIC_API_BASE=http://localhost:8010
